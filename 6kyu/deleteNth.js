

var deleteNth = (arr, n) => {
	let holder = [];
	for(var i = 0; i < arr.length; i++) {
		let counter = 0;
		for(var j = 1; j < arr.length; j++) {
			if(arr[i] === arr[j]) {
				counter++
			}
		}
	}	
}